# React CRUD Application - Server

This is the server side of the React CRUD application.  To run the server enter the command `node server.js` in your console window when navigated within this directory.
